exports.handler=async(event)=>{if(event.httpMethod!=='POST')return{statusCode:405,body:'Method Not Allowed'};const key=process.env.GEMINI_API_KEY;if(!key)return{statusCode:500,body:JSON.stringify({error:'GEMINI_API_KEY is not configured in Netlify'})};try{const body=JSON.parse(event.body||'{}');const prompt=String(body.prompt||'').trim();if(!prompt)return{statusCode:400,body:JSON.stringify({error:'Prompt is required'})};const schema={type:'object',properties:{type:{type:'string',enum:['generate_timetable','schedule_course','delete_timetable_entry','validate','report','none']},course_code:{type:'string'},course:{type:'string'},teacher:{type:'string'},program:{type:'string'},semester:{type:'string'},section:{type:'string'},day:{type:'string',enum:['Monday','Tuesday','Wednesday','Thursday','Friday']},start_time:{type:'string'},duration_slots:{type:'integer',minimum:1,maximum:3},session_type:{type:'string',enum:['Theory','Lab']},resource:{type:'string'},entry_id:{type:'string'},report:{type:'string'},message:{type:'string'}},required:['type'],additionalProperties:false};const system=`You are the UVAS Conflict-Free Timetable command interpreter. Convert the user's natural-language request into ONE JSON action. You do not modify data. A deterministic application validates every mutation. Hard rules: Monday-Friday only; 50-minute slots; theory is exactly 1 or 2 consecutive slots; lab is exactly 3 consecutive slots; no teacher, section, classroom or lab overlap; unavailable faculty periods are forbidden. Do not invent official course codes or IDs. If the request is ambiguous, return type none with a concise message. For generation use generate_timetable. For a specific allocation use schedule_course and include course_code when supplied, teacher, section, day, start_time, duration_slots, session_type and resource when supplied. For deletion use delete_timetable_entry only when an entry_id is explicitly provided. For workload/conflict/free-resource questions use report or validate.`;const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.7-flash:generateContent',{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':key},body:JSON.stringify({systemInstruction:{parts:[{text:system}]},contents:[{role:'user',parts:[{text:JSON.stringify({prompt,context:body.data||{}})}]}],generationConfig:{responseMimeType:'application/json',responseSchema:schema,temperature:0}})});const j=await r.json();if(!r.ok)return{statusCode:r.status,body:JSON.stringify({error:j.error?.message||'Gemini request failed'})};const text=j.candidates?.[0]?.content?.parts?.[0]?.text||'{}';let action;try{action=JSON.parse(text)}catch{return{statusCode:502,body:JSON.stringify({error:'Gemini returned malformed JSON'})}};return{statusCode:200,headers:{'Content-Type':'application/json'},body:JSON.stringify({action})}}catch(e){return{statusCode:500,body:JSON.stringify({error:e.message})}}};